<%- include('../partials/header', { title: 'Registro de proveedor' }) %>

<h1 class="page-title">Registro de proveedor</h1>
<p class="page-subtitle">
  Creá tu perfil de oficio para publicar servicios y recibir reseñas.
</p>

<div class="card form-card">
  <% if (errorMessage) { %>
    <div class="alert-error">
      <%= errorMessage %>
    </div>
  <% } %>

  <form action="/auth/register" method="POST">
    <div class="form-group">
      <label for="name" class="form-label">Nombre</label>
      <input
        type="text"
        id="name"
        name="name"
        class="form-input"
        placeholder="Tu nombre o nombre del emprendimiento"
      />
    </div>

    <div class="form-group">
      <label for="email" class="form-label">Correo electrónico</label>
      <input
        type="email"
        id="email"
        name="email"
        class="form-input"
        required
      />
    </div>

    <div class="form-group">
      <label for="password" class="form-label">Contraseña</label>
      <input
        type="password"
        id="password"
        name="password"
        class="form-input"
        minlength="6"
        required
      />
      <p class="form-help">Mínimo 6 caracteres.</p>
    </div>

    <div class="form-actions">
      <button type="submit" class="btn btn-primary">
        Registrarse como proveedor
      </button>
    </div>
  </form>
</div>

<p class="text-sm">
  ¿Querés registrarte como cliente?
  <a href="/auth/register-client" class="link-muted">Registro de cliente</a>
</p>

<p class="text-sm">
  ¿Ya tenés cuenta?
  <a href="/auth/login" class="link-muted">Iniciar sesión</a>
</p>

<%- include('../partials/footer') %>
